{
  "name": "fcm-api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:migrate": "bun run src/scripts/migrate.ts"
  },
  "dependencies": {
    "firebase-admin": "^13.1.0",
    "hono": "^4.6.20",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
