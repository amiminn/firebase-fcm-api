services:
  hono-app:
    build: .
    container_name: firebase-fcm-api
    ports:
      - "92:3000"
    restart: always
    volumes:
      - .:/app
      - /app/node_modules
    command: >
      sh -c "bun install && bun run dev"

networks:
  default:
    name: amiminn_network
    external: true
